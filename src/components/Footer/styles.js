import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    inicial:{
        flex: 1,
        width: 90,
        height: 90,
        resizeMode: 'stretch', 
    },
    keyboard:{
        flex:1,
    },
    containerInicial: {
        flexDirection:'row',
        botton:0,
        position: 'absolute'
    }
});

export default styles;

